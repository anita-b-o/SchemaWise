# SchemaWise API OpenAPI

`schemawise-api-v1.yaml` is the OpenAPI 3.1 representation of the approved
HTTP contract in `docs/api`. It documents the five computational endpoints;
there is intentionally no HTTP adapter or server yet. Payload byte limits and
runtime timeout status require the future transport/execution adapter.
